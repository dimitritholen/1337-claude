---
max_turns: 3
allowed_tools: [Read, Glob, Grep]
---

Read the output style `output-styles/unc.md` in the current directory and
reply in exactly that style to this question:

My pytest run says `ModuleNotFoundError: No module named 'requests'`, but I
ran `pip install requests` ten minutes ago. Why?
